// lib/style/app_styles.dart
import 'package:flutter/material.dart';

class AppStyles {
    // Цвета
    static const Color primary = Color(0xFF1E1E2F);      // тёмно-синий/фиолетовый
    static const Color secondary = Color(0xFF3A3A5C);    // синий акцент
    static const Color accent = Color(0xFFBB86FC);       // светлый фиолетовый
    static const Color background = Color(0xFF121212);   // почти черный
    static const Color textPrimary = Color(0xFFE0E0E0);  // светлый серый
    static const Color textSecondary = Color(0xFFB0B0B0);// серый для вспомогательного текста
    // Шрифты
    static const String fontFamily = 'Roboto';
}
